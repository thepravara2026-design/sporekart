apiVersion: apps/v1
kind: Deployment
metadata:
  name: cart-service
  namespace: sporekart-services
  labels:
    app: cart-service
    component: cart
    environment: production
    managed-by: terraform
spec:
  replicas: 2
  selector:
    matchLabels:
      app: cart-service
  template:
    metadata:
      labels:
        app: cart-service
        component: cart
      annotations:
        prometheus.io/scrape: "true"
        prometheus.io/port: "8080"
    spec:
      containers:
        - name: cart
          image: 123456.dkr.ecr.us-east-1.amazonaws.com/sporekart-cart:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 8080
          envFrom:
            - configMapRef:
                name: sporekart-global-config
            - configMapRef:
                name: sporekart-database-config
            - configMapRef:
                name: sporekart-observability-config
          resources:
            requests:
              cpu: 128m
              memory: 256Mi
            limits:
              cpu: 256m
              memory: 512Mi
          livenessProbe:
            httpGet:
              path: /actuator/health/liveness
              port: 8080
            initialDelaySeconds: 30
            periodSeconds: 15
          readinessProbe:
            httpGet:
              path: /actuator/health/readiness
              port: 8080
            initialDelaySeconds: 20
            periodSeconds: 10
          startupProbe:
            httpGet:
              path: /actuator/health
              port: 8080
            initialDelaySeconds: 10
            periodSeconds: 5
            failureThreshold: 30
---
apiVersion: v1
kind: Service
metadata:
  name: cart-service
  namespace: sporekart-services
  labels:
    app: cart-service
    component: cart
spec:
  selector:
    app: cart-service
  ports:
    - name: http
      port: 8080
      targetPort: 8080
  type: ClusterIP
