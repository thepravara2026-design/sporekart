# AsyncAPI Contracts

The following event contracts are now defined for Sprint 1 implementation readiness:

- Identity Service: [identity-service.yaml](identity-service.yaml)

Each contract follows the enterprise AsyncAPI baseline and documents the owned event stream and payload schema.
