# OpenAPI Contracts

The following service contracts are now defined for Sprint 1 implementation readiness:

- Identity Service: [identity-service.yaml](identity-service.yaml)

Each contract follows the enterprise OpenAPI 3.1 baseline and should remain the source of truth for implementation.
